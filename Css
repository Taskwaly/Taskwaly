body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

.screen {
    display: none;
    padding: 20px;
    max-width: 600px;
    margin: auto;
}

h1 {
    text-align: center;
}

form {
    display: flex;
    flex-direction: column;
    align-items: center;
}

input, select, button {
    margin: 10px;
}

.top-bar {
    display: flex;
    justify-content: space-between;
}

button {
    padding: 10px 20px;
}
